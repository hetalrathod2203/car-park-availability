spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password: 
  
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
  
  h2:
    console:
      enabled: true
  
  liquibase:
    enabled: false

carpark:
  api:
    url: http://localhost:8090/mock-api
  onemap:
    api:
      url: http://localhost:8090/mock-onemap